-- ============================================================
--  V19 — 쓰이지 않는 컬럼 제거
-- ============================================================

-- 실행 16건 전부 NULL 이고, 크롤러의 start_job() 은 이 컬럼을 INSERT 하지
-- 않습니다. 수집 단위가 「월」에서 「지역 + 최근 N개월」로 바뀌면서 남은
-- 컬럼입니다. 기간은 --months 인자로 넘기고 DB 에 남기지 않습니다.
ALTER TABLE crawl_jobs DROP COLUMN IF EXISTS target_year_month;
