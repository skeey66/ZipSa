# ZipSa 협업 규칙

> 이 문서는 팀이 함께 지키기로 한 약속입니다.
> 바꾸고 싶은 규칙이 생기면 **이 파일을 수정하는 PR**을 올려서 논의합니다. 규칙도 코드처럼 리뷰합니다.

---

## 0. 프로젝트 구조

| 디렉터리 | 내용 | 주 언어 |
| --- | --- | --- |
| `backend/` | Spring Boot API 서버 | Java |
| `frontend/` | Vue.js 웹 클라이언트 | TypeScript / JavaScript |
| `crawler/` | 매물 수집 크롤러 | Python |

작업 영역이 다르더라도 **하나의 리포지토리, 하나의 규칙**으로 움직입니다.

---

## 1. 브랜치 전략 — GitHub Flow

```
main ──●──────●──────●──────●─────▶  (항상 배포 가능한 상태)
        \    /  \    /
         feature  feature
```

### 규칙

- **`main` 에 직접 push 하지 않습니다.** 모든 변경은 PR을 거칩니다.
- `main` 은 언제나 **빌드가 통과하고 실행 가능한 상태**를 유지합니다.
- 브랜치는 **작업 단위로 새로 만들고, 머지되면 삭제**합니다. 오래 살아있는 브랜치를 만들지 않습니다.

### 브랜치 이름

```
<타입>/<기능-영문-설명>
```

**브랜치 이름에 번호를 넣지 않습니다.** 이름만 보고 무슨 작업인지 알 수 있어야 합니다.
머지 순서는 `docs/collab/협업-시나리오.md` 가 관리합니다.

| 타입 | 용도 |
| --- | --- |
| `feature` | 새 기능 |
| `fix` | 버그 수정 |
| `refactor` | 동작 변경 없는 구조 개선 |
| `docs` | 문서 |
| `chore` | 빌드 설정, 의존성, 잡일 |

예시

```
feature/ai-insight-api
fix/crawler-timeout
docs/api-spec
```

**영문 소문자와 하이픈만 씁니다.** 한글이나 `#` 을 넣어도 git 은 받아주지만, 브랜치 URL 이 `%23` 으로 깨져서 링크 공유가 지저분해지고, 나중에 CI 에서 브랜치명을 Docker 태그나 아티팩트 이름으로 쓸 때 깨집니다.

### 작업 시작 루틴

```bash
git checkout main
git pull origin main
git checkout -b feature/ai-insight-api
```

---

## 2. 작업 단위 규칙

- **이슈는 쓰지 않습니다.** 작업 단위는 `docs/collab/협업-시나리오.md` 의 순번(①~⑫)으로 정해져 있습니다.
- **기능 하나 = 브랜치 하나 = PR 하나.** 브랜치에 담을 파일은 `docs/collab/manifest/*.txt` 에 적혀 있습니다.
  - 예외: PR 이 너무 커져서 나누는 경우(4장 참고)는 **기능 하나에 PR 여러 개**여도 괜찮습니다.
- PR 을 올리면 **본인을 Assignee 로 지정**합니다. 지정되지 않은 PR 은 아무도 안 하는 작업입니다.
- 시나리오에 없는 일이 생기면(버그·급한 수정) `fix/` 브랜치를 새로 파고 PR 제목에 무엇을 고쳤는지 씁니다.

### PR 제목

```
[영역] 무엇을 했는가
```

예시

```
[backend] 매물 검색 API 구현
[crawler] 네이버 부동산 수집 시 timeout 처리
[frontend] 매물 상세 페이지 레이아웃
```

### 라벨

`backend` · `frontend` · `crawler` · `infra` · `bug` · `docs` · `help wanted`

막혀서 도움이 필요하면 `help wanted` 라벨을 붙이고 팀 채널에 공유합니다.

---

## 3. 커밋 컨벤션 — Conventional Commits

```
<타입>(<범위>): <요약>

<본문 - 선택>
```

### 타입

| 타입 | 의미 |
| --- | --- |
| `feat` | 새 기능 |
| `fix` | 버그 수정 |
| `refactor` | 동작은 그대로, 코드 구조 개선 |
| `test` | 테스트 추가/수정 |
| `docs` | 문서 |
| `style` | 포맷팅, 세미콜론 등 (로직 변경 없음) |
| `chore` | 빌드, 의존성, 설정 |

### 범위(scope)

`backend` · `frontend` · `crawler` · `infra` · `docs`

### 작성 규칙

- 요약은 **한글로, 명령형, 50자 이내**, 마침표 없이.
- **왜 그렇게 했는지**가 코드에서 안 보이면 본문에 적습니다. 무엇을 했는지는 diff 가 이미 말해줍니다.
- 커밋 하나에 한 가지 의도만 담습니다. 기능 추가와 포맷팅을 섞지 않습니다.

### 예시

```
feat(backend): 매물 검색 API 추가

지역·가격·평수 필터를 조합해 조회할 수 있게 함.
페이징은 기본 20건, 최대 100건으로 제한.
```

```
fix(crawler): 응답 지연 시 세션이 끊기던 문제 수정
refactor(frontend): 매물 카드 컴포넌트 props 정리
chore(backend): Spring Boot 3.3.0 으로 버전 고정
```

---

## 4. Pull Request 규칙

### PR 은 작게

- **변경 400줄 이내를 권장**합니다. 리뷰어가 집중할 수 있는 한계입니다.
- 커지면 "기반 작업 PR → 기능 PR" 로 쪼갭니다. 큰 PR 하나보다 작은 PR 셋이 훨씬 빨리 머지됩니다.

### PR 제목

**커밋 컨벤션과 똑같은 형식으로 씁니다.**

```
feat(backend): 매물 검색 API 추가
```

PR 제목은 머지 커밋 메시지에 그대로 들어갑니다. 대충 지으면 3장에서 정한 커밋 컨벤션이
`main` 히스토리에 남지 않습니다.

### PR 본문 양식

```markdown
## 변경 내용
- 무엇을 왜 바꿨는지 한두 줄

## 테스트 방법
- 리뷰어가 직접 확인할 수 있는 절차 (명령어 / URL / 입력값)

## 스크린샷
- UI 변경이면 필수

## 담긴 파일
- `docs/collab/manifest/3-AI1-ai-insight.txt` (9개)
```

### 머지 조건

1. **리뷰어 1명 이상 Approve**
2. 빌드 / 테스트 통과
3. `main` 과 충돌 없음

> GitHub Actions 를 붙이기 전까지 2번은 **각자 로컬에서 확인하는 자율 점검**입니다.
> CI 를 추가하면 그때부터 자동으로 막힙니다.

### 머지 방식

- **Create a merge commit 으로 통일**합니다. 브랜치가 갈라졌다 합쳐진 그래프가 `main` 에 남습니다.
  (Squash 로 합치면 그래프가 일직선이 되어 누가 언제 무엇을 병렬로 했는지 안 보입니다.)
- 머지는 **PR 작성자가 직접** 합니다. 승인받았다고 리뷰어가 대신 머지하지 않습니다.
- 머지 후 **브랜치는 삭제**합니다.

### 작성 중인 PR

아직 리뷰받을 준비가 안 됐으면 **Draft PR** 로 올립니다. 진행 상황이 공유되면서 리뷰 요청은 아니라는 뜻이 됩니다.

---

## 5. 코드 리뷰 규칙

### 리뷰어 지정

- PR 을 올린 사람이 **리뷰어를 직접 지정**합니다. 아무도 지정하지 않은 PR 은 아무도 보지 않습니다.
- 기본은 **같은 영역을 맡은 사람 1명**입니다. (backend PR → backend 작업자)
- 그 영역에 나 혼자라면 **아무나 1명**을 지정합니다. 코드를 몰라도 "이 부분은 설명이 필요하다" 는 충분히 좋은 리뷰입니다.

### 리뷰어가 할 일

- 리뷰 요청을 받으면 **24시간 안에 1차 반응**을 남깁니다. 자세히 못 보더라도 "오늘 저녁에 볼게요" 라도 남깁니다. **침묵이 가장 나쁩니다.**
- 코멘트에는 등급을 붙여서, 작성자가 뭘 꼭 고쳐야 하는지 알 수 있게 합니다.

| 태그 | 의미 |
| --- | --- |
| `[P1]` | 반드시 고쳐야 함. 고치기 전엔 Approve 안 함 |
| `[P2]` | 제안. 반영은 작성자 판단 |
| `[Q]` | 질문. 이해가 안 가서 묻는 것 |

예시

```
[P1] 이 쿼리는 매물 수가 늘면 N+1 이 납니다. fetch join 필요해 보여요.
[P2] 이 부분 메서드로 빼면 읽기 편할 것 같아요.
[Q] 여기서 null 이 들어올 수 있는 경우가 있나요?
```

### 작성자

- 모든 코멘트에 **답을 남깁니다.** 반영했으면 반영했다고, 안 할 거면 왜 안 하는지.
- 동의하지 않으면 그냥 따르지 말고 **근거를 들어 이야기합니다.** 리뷰는 합의 과정이지 지시가 아닙니다.

### 함께 지킬 것

- **리뷰는 코드에 대한 것이지 사람에 대한 것이 아닙니다.** "이건 왜 이렇게 했어요?" 가 아니라 "이 부분 이렇게 하면 어떨까요?" 로 씁니다.
- 좋은 코드를 봤으면 그것도 남깁니다. 리뷰는 지적하는 자리만은 아닙니다.

---

## 6. 충돌과 동기화

- **작업 중 `main` 이 바뀌면 자주 가져옵니다.** 오래 묵힐수록 충돌이 커집니다.

```bash
git checkout main && git pull origin main
git checkout feature/ai-insight-api
git rebase main
```

- **충돌은 브랜치 작성자가 해결합니다.** 리뷰어에게 넘기지 않습니다.
- **내 브랜치에서만 rebase 합니다.** 이미 push 한 브랜치를 rebase 했다면 `git push --force-with-lease` 를 씁니다. (`--force` 말고 `--force-with-lease`)
- 충돌이 감당 안 되면 혼자 붙잡지 말고 팀 채널에 공유합니다. 화면 공유하며 같이 푸는 게 빠릅니다.

---

## 7. 코드 스타일

포맷 때문에 리뷰에서 다투지 않도록, **도구가 정하고 사람은 로직만 봅니다.**

### `backend/` — Java / Spring Boot

- **Google Java Style** 을 따릅니다. IDE 에 포매터를 설정해두고 저장 시 자동 적용합니다.
- 들여쓰기 2칸, **한 줄 최대 100자** (Google Java Style 의 column limit).
- 패키지 구조는 **도메인 단위**로 나눕니다. (`property/`, `user/`, `search/` — `controller/`, `service/` 로 먼저 나누지 않습니다)
- 클래스 `PascalCase`, 메서드/변수 `camelCase`, 상수 `UPPER_SNAKE_CASE`.
- 테스트는 **JUnit 5**, 클래스명은 `대상클래스Test`. 새 비즈니스 로직에는 테스트를 함께 올립니다.
- `System.out.println` 대신 로거를 씁니다.

```bash
./gradlew build   # 빌드 + 테스트. PR 올리기 전 필수
./gradlew test
```

### `frontend/` — Vue.js

- **ESLint + Prettier** 로 통일합니다. 커밋 전 `npm run lint` 를 돌립니다.
- **Composition API + `<script setup>`** 을 기본으로 씁니다.
- 컴포넌트 파일명은 `PascalCase.vue` (`PropertyCard.vue`), 두 단어 이상으로 짓습니다.
- 컴포넌트 안 순서는 `<script setup>` → `<template>` → `<style scoped>`.
- 스타일은 **`scoped`** 로 씁니다. 전역 스타일은 공용 파일에만.
- API 호출은 컴포넌트에 직접 쓰지 않고 `api/` 모듈을 통해 부릅니다.

```bash
npm run lint
npm run build   # PR 올리기 전 필수
```

### `crawler/` — Python

- **ruff + black** 으로 통일합니다. 들여쓰기 4칸, PEP 8.
- 함수/변수 `snake_case`, 클래스 `PascalCase`.
- **타입 힌트를 붙입니다.** 특히 함수 시그니처.
- 의존성은 `requirements.txt` 에 버전을 고정해서 적습니다.
- 크롤링 대상 사이트의 `robots.txt` 와 요청 간격을 지킵니다. 과도한 요청은 팀 전체가 차단당하는 결과로 돌아옵니다.

```bash
ruff check .
black .
pytest
```

---

## 8. 하면 안 되는 것

| 금지 | 이유 |
| --- | --- |
| `.env`, API 키, 비밀번호, 인증서 커밋 | 한 번 올라가면 히스토리에 영원히 남습니다. 실수했으면 **즉시 팀에 알리고 키를 폐기·재발급**합니다 |
| `main` 에 직접 push / force push | 되돌릴 수 없는 사고가 납니다 |
| 남의 브랜치를 말없이 수정하거나 force push | 상대의 작업이 사라집니다. 도와줄 땐 먼저 물어봅니다 |
| 리뷰 없이 머지 | 규칙의 존재 이유가 사라집니다 |
| 대용량 파일(영상, 덤프, 빌드 산출물) 커밋 | 클론이 느려지고 되돌리기 어렵습니다 |
| 내 PR 과 무관한 파일 함께 수정 | 리뷰가 불가능해집니다. 별도 PR 로 올립니다 |

`.env` 는 커밋하지 않되, **`.env.example` 은 커밋**해서 필요한 키 목록을 팀이 알 수 있게 합니다.

사고를 규칙이 아니라 **설정으로 막습니다.** 리포 루트의 `.gitignore` 에 `.env`, 빌드 산출물, `node_modules/`, `__pycache__/` 가 들어 있는지 먼저 확인하세요.
실수로 이미 커밋했다면 파일만 지우는 걸로는 부족합니다 — **히스토리에 그대로 남아 있으므로 해당 키를 즉시 폐기하고 재발급**해야 합니다.

---

## 9. 커뮤니케이션

- **30분 룰** — 같은 문제에 30분 넘게 막혔으면 팀 채널에 공유합니다. 혼자 붙잡고 있는 게 팀에 더 손해입니다.
- **매일 짧게 공유** — 어제 한 것 / 오늘 할 것 / 막힌 것. 길게 쓸 필요 없습니다.
- **결정은 글로 남깁니다.** 통화나 대면으로 정한 것도 PR 코멘트에 한 줄 남깁니다. 안 남기면 없던 일이 됩니다.
- **일정이 밀릴 것 같으면 미리 말합니다.** 마감일에 말하는 것보다 3일 전에 말하는 게 훨씬 낫습니다.

---

## 10. 요약 체크리스트

작업 시작할 때

- [ ] 앞 작업이 머지된 `main` 최신 상태에서 브랜치를 땄는가?
- [ ] 내 manifest 파일만 스테이징했는가? (`git status --short` 로 확인)

PR 올리기 전

- [ ] 빌드/테스트가 통과하는가?
- [ ] 린트를 돌렸는가?
- [ ] `.env` 나 키가 섞여 들어가지 않았는가?
- [ ] 변경이 400줄을 넘지 않는가?
- [ ] **PR 제목**을 커밋 컨벤션 형식으로 썼는가?
- [ ] PR 본문에 테스트 방법을 썼는가?
- [ ] 리뷰어를 지정했는가?

머지한 뒤

- [ ] 브랜치를 삭제했는가?

---

## 부록. 리포지토리 설정 (최초 1회)

**이 문서의 규칙 중 절반은 글이 아니라 설정으로 강제됩니다.** 리포를 만든 사람이 아래를 한 번 켜두면, 나머지는 GitHub 이 대신 막아줍니다.

### Settings → Branches → Add branch protection rule

브랜치 패턴에 `main` 을 넣고:

- [ ] **Require a pull request before merging** — `main` 직접 push 차단
  - [ ] Require approvals: **1**
- [ ] **Do not allow bypassing the above settings** — 관리자도 예외 없음

### Settings → General → Pull Requests

- [ ] **Allow merge commits** 만 체크, Squash / Rebase merging 은 **해제**
- [ ] **Automatically delete head branches** — 머지된 브랜치 자동 삭제

### Settings → Collaborators

- [ ] 팀원 4~5명을 **Write** 권한으로 초대

### 라벨

Issues → Labels 에서 2장의 라벨(`backend` `frontend` `crawler` `infra` `bug` `docs` `help wanted`)을 미리 만들어 둡니다.
